Classes:

	User Interface
		Lucy and Josh
		
		StartPage() //Should we have seperate class for the start page, within connectFour.run call this class and wait to get difficulty/person v. person options?   
		BoardRenderer(Board)		
		MoveInputter() //Needs a better name
		connectFour()  //Main
		
	AI - Game Player
		Kirsten and Michelle
		
		AI_Player(int difficulty)
			int nextMove(Board currentGame)

	Game Simulation and Processing
		Peng

		Board()
		GameSimulator(Players)
